# Firebase Admin SDK 設定（僅供遷移腳本使用，不要提交至 git）
# 從 Firebase Console > Project Settings > Service Accounts > Generate new private key 下載 JSON
# 複製此檔案為 .env.migration 並填入實際值

FIREBASE_PROJECT_ID=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json
